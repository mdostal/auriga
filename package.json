{
  "name": "auriga",
  "version": "0.1.0",
  "private": true,
  "description": "Auriga — Pantheon auto-router. Root test entrypoint so CI's generic Node gate (ci.yml) actually runs the router's unit + loop-level e2e suite.",
  "scripts": {
    "test": "node --test src/router/test/*.test.mjs"
  }
}

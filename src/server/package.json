{
  "name": "auriga-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Auriga server — read-only node:http JSON API over .pHive/ state (epics, stories, activity). Zero runtime dependencies.",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
